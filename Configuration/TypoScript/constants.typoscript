# Compatibility fallback for legacy static TypoScript includes.
# Prefer the Site Set `erhaweb/feed-display` in TYPO3 v13 and v14.
@import 'EXT:feed_display/Configuration/Sets/FeedDisplay/constants.typoscript'
